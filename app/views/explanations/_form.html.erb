<div class="artpost-container" style="margin-top: 10rem; height: 1000px;">
  <h2>Edita tu explicación</h2>

  <%= simple_form_for [@art, @explanation] do |f| %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

    <div class="my-5 form-inputs d-flex gap-5">
      <div class='col-6 d-flex'>
        <%# Formulario %>
        <div class="w-100">
          <%= f.input :description, placeholder: "Add a description here...", label: false, input_html: { style: 'background-color: white; height: 10rem;' } %>
          <%= f.input :image, as: :file, required: true, label: false, placeholder: "Image", id: "upload_widget", class: "cloudinary-button", input_html: { style: 'background-color: white' } %>
          <%= f.submit "Submit", data: {turbo_action: "advance", turbo_frame: "_top"} , class: "mt-4 btn btn-dark" %>
        </div>
      </div>

      <%# Imagen %>
      <div id="imageContainer" style="position: relative;">
        <%= cl_image_tag(explanation.image.key, id: "image", class: "img-fluid", alt: "Imagen de arte", style: "max-width: 100%;") %>
        <div id="pointer" style="position: absolute; width: 20px; height: 20px; background: red; display: none;"></div>
      </div>
    </div>
  <% end %>
</div>
