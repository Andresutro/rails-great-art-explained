<div class="container my-5">
  <div class="card">
    <div class="card-header">
      Detalles de la donación
    </div>
    <div class="card-body">
      <h5 class="card-title">Donación Pendiente</h5>
      <p class="card-text">
        <strong>Monto:</strong> <%= @donation.amount %>
      </p>
      <p class="card-text">
        <strong>Donante:</strong> <%= @donation.user.email %>
      </p>
      <p class="card-text">
        <strong>Donante:</strong> <%= @donation.recipient.email %>
      </p>

    </div>
  </div>
  <div class="mt-3">
    <%= link_to "Volver", root_path, class: "btn btn-primary" %>
  </div>
</div>

<script src="https://sdk.mercadopago.com/js/v2"></script>

<!-- Contenedor donde se colocará el botón de pago -->
<div id="wallet_container"></div>


<script>
  const mp = new MercadoPago('TEST-2a56a125-0912-4516-95a3-96947efeb336');
  const bricksBuilder = mp.bricks();
  const preferenceId = "<%= @preference_id %>";

  mp.bricks().create("wallet", "wallet_container", {
    initialization: {
        preferenceId: preferenceId,
        redirectMode: "modal"

    },
  });

</script>
